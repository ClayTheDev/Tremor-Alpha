name: Tremor
version: '${project.version}'
main: me.clay.commands2.Commands2
api-version: 1.19
commands:
  Zeus:
    description: become one with the gods.
    usage: /<command>
  hunger:
    description: gives player hunger with the amplifier of 10.
    usage: /<command>
  ping:
    description: Shows user's Ping.
    usage: /<command>
    aliases:
      - p
  player:
    description: Shows user information. EX; Shows IGN, Ping, And other.
    usage: /<command>
  kicker:
    description: kicks the user!!!!
    usage: /<command>
  sex:
    description: It doesn't do what the name says, Don't do it.
    usage: /<command>
  shell:
    description: Your a turtle now. Don't die!!
    usage: /<command>
  announce:
    description: Announces a broadcasted message to the server.
    usage: /<command>
    aliases:
      - ann
      - broadcast
      - sbc